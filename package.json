{
  "name": "aaply-test",
  "version": "1.0.0",
  "private": true,
  "homepage": "aaply-test/",
  "dependencies": {
    "canvg": "^3.0.7",
    "konva": "^7.2.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-konva": "^17.0.1-3",
    "react-scripts": "4.0.1"
  },
  "devDependencies": {
    "husky": "^4.3.8",
    "prettier": "^2.2.1",
    "pretty-quick": "^3.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && mkdir -p docs && cp -r build/* docs/"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
